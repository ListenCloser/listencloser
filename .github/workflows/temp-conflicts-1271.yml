name: Temporary sync 1271

on:
  push:
    branches: [feat/1193-corrected-playback]

permissions:
  contents: write

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version-file: package.json
          cache: npm
      - name: Merge current main and resolve correction continuation
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git merge --no-commit --no-ff origin/main || true

          conflicts="$(git diff --name-only --diff-filter=U)"
          expected=$'backend/domain/correction_entity_sync.py\nbackend/tests/integration/test_correction_lineage.py\nbackend/tests/test_correction_entity_sync.py\ncomponents/workspace/PianoRollCorrectionCoordinator.tsx\ncomponents/workspace/representations/registry.tsx\ndocs/generated/frontend-dependencies.md'
          if [[ "$conflicts" != "$expected" ]]; then
            echo "Unexpected conflict set:" >&2
            printf '%s\n' "$conflicts" >&2
            exit 1
          fi

          git checkout --ours -- backend/domain/correction_entity_sync.py backend/tests/integration/test_correction_lineage.py backend/tests/test_correction_entity_sync.py components/workspace/PianoRollCorrectionCoordinator.tsx components/workspace/representations/registry.tsx
          git add backend/domain/correction_entity_sync.py backend/tests/integration/test_correction_lineage.py backend/tests/test_correction_entity_sync.py components/workspace/PianoRollCorrectionCoordinator.tsx components/workspace/representations/registry.tsx
          git checkout --ours -- docs/generated/frontend-dependencies.md
          git add docs/generated/frontend-dependencies.md

          npm ci
          output_dir="$(mktemp -d)"
          npx --yes --package=dependency-cruiser@18.2.0 --package=typescript@5.7.3 depcruise --config .dependency-cruiser.cjs --output-type mermaid --include-only '^(app|components|lib)/' --collapse 2 --output-to "$output_dir/frontend-dependencies.mmd" app components lib
          printf '%s\n' '# Frontend dependency graph' '' '> Generated by dependency-cruiser 18.2.0 with TypeScript 5.7.3 from `.dependency-cruiser.cjs`. Do not hand-edit this graph; regenerate it through the architecture check when frontend imports change.' '' 'This is an orientation view of `app/`, `components/`, and `lib/`. Deeper folders are collapsed to keep the graph reviewable. The architecture rules in `.dependency-cruiser.cjs`, not the picture alone, are the enforceable contract.' '' '```mermaid' > docs/generated/frontend-dependencies.md
          cat "$output_dir/frontend-dependencies.mmd" >> docs/generated/frontend-dependencies.md
          printf '%s\n' '```' >> docs/generated/frontend-dependencies.md
          rm -rf "$output_dir"
          git add docs/generated/frontend-dependencies.md

          rm .github/workflows/temp-conflicts-1271.yml
          git add .github/workflows/temp-conflicts-1271.yml
          test -z "$(git diff --name-only --diff-filter=U)"
          git diff --cached --check
          git commit -m "chore: sync corrected playback with current main"
          git push origin HEAD:feat/1193-corrected-playback
