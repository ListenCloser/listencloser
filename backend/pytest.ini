[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = strict
markers =
    integration: tests that require a real Supabase/Postgres database (run via the database-integration CI job)
addopts = -m "not integration"
