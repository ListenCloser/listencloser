# Deprecated engineering constitution

> **Status: superseded.** Retained only to avoid breaking old links.

The canonical engineering and autonomous-agent rules are in the repository-root [`AGENTS.md`](../AGENTS.md). Documentation authority is defined in [`docs/README.md`](README.md).

This older file previously contained a priority ordering that placed simplicity and maintainability ahead of correctness. That is not the repository contract. The current policy treats correctness, security, evidence/truthfulness, production verification, simplicity, and maintainability as constraints to balance; none may be silently sacrificed merely to reduce code or obtain green CI.

Do not add new rules here. Update root `AGENTS.md` when the engineering contract intentionally changes.
